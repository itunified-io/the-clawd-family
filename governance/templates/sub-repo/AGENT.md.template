# AGENT.md – {{REPO_NAME}} Project Rules

**Repository:** itunified-io/{{REPO_NAME}}
**Jira Project:** {{JIRA_PROJECT_KEY}}
**Confluence Space:** {{CONFLUENCE_SPACE_KEY}}
**Version:** 1.0.0
**Last Updated:** {{DATE}}

---

## Governance Source

This repository is governed by The Clawd Family central governance.

**RAG API (preferred):**
```
POST {{DASHBOARD_URL}}/api/governance/search
Content-Type: application/json

{ "query": "<your question>", "role": "<YOUR_ROLE>", "limit": 5 }
```

**Version check:**
```
GET {{DASHBOARD_URL}}/api/governance/version
```

**Fallback:** [the-clawd-family/AGENT.md](https://github.com/itunified-io/the-clawd-family/blob/main/AGENT.md)

### Session Start

At the beginning of every agent session, query the governance API:
1. `GET /api/governance/version` — check governance version
2. `POST /api/governance/search` — query for role-specific policies with `{ "query": "session start checklist", "role": "<YOUR_ROLE>" }`
3. If the API is unreachable, fall back to the raw AGENT.md link above

---

## Scope

This repository contains {{REPO_DESCRIPTION}}.

---

## Project Structure

```
{{REPO_NAME}}/
├── README.md
├── AGENT.md
├── CLAUDE.md
├── src/
├── tests/
└── docker-compose.yml
```

---

## Repo-Specific Rules

> Add repo-specific additive rules below. These extend (never override) master governance.

---

## Component Registry (this repo)

| Component | Code Paths | Owner Role(s) | Jira Label |
|-----------|------------|---------------|------------|
| `{{PREFIX}}-example` | `src/` | BACKEND | `comp:{{PREFIX}}-example` |

---

Refer to the governance RAG API above, or [the-clawd-family/AGENT.md](https://github.com/itunified-io/the-clawd-family/blob/main/AGENT.md) for all other governance rules.
